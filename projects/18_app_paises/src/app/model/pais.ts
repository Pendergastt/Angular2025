export class Pais{
// cambiamos los nombres porque desde el rest que hemos creado vienen así.
  nombre:string;
  capital:string;
  continente:string;
  poblacion:number;
  bandera:string;
//no hace falta constructor porque es simplemente traer datos, no construirlos. simplemente
//le digo la estructura de los datos que va a tener

}
